This package requires python 2.4 or higher and libxml2 with python bindings to
build and run.

psyco will be used if present. It gives a ~40% decrease in runtime.

To install:

    python setup.py install


WINDOWS:

To get libxml2 with python bindings:
    http://users.skynet.be/sbi/libxml-python/
