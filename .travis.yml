language: python

python:
  - "2.7"
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"

install:
    - pip install coveralls

script:
    - nosetests --with-coverage --cover-tests --cover-package=pyx12 --cover-erase

after_success:
    - coveralls
